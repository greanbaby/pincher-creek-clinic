<html>
    <head>
        <%- include( 'headerCommon' ); %>
    </head>
    <body>
        <%- include( 'headerPages' ); %>

        <p>Welcome to <%= display.title %></p>

        <p>
            <!-- NOTESLIST -->
            <% if (noteslist.length > 0) { %>
                SOMETHINGS!!
            <% } %>

        </p>

        <!-- SHEETS -->
        <p>Here is a link to the sheets page:
            <a href='/sheets'>SHEETS</a>
        </p>

    <%- include( 'footerCommon' ); %>
